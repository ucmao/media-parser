# 松果时刻解析指南

## 支持范围

- 分享短链：`https://m.pineconemoment.com/o/{短码}`。
- 展开后的故事页：`/h5/share/story/{作品ID}`。

## 提取字段

可提取标题、作者、封面、每页无水印 MP4、页面原图与作品合成配音音频。

## 实现说明

短链跳转后会携带作品、作者与分享参数。解析器调用分享页自身使用的公开详情接口，并优先返回每页的 `video_url`；同时保留 `images` 和 `dubbing.h5_audio`。
